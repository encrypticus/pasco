mixin header(active = '1')
  header.page-header
    .page-header__overlay
      .wrapper.wrapper_flex
        .page-header__hamburger
          +hamburger
        +mmenu
        +logo
        .page-header__nav
          +main-nav(active)
        .page-header__search
          +header-search