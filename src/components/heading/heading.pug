mixin heading(text, level = 1)
  if (level === 1)
    h1.heading 5 Ways milllennials can start<br/>building their future today
  else
    h2.heading 5 Ways milllennials can start<br/>building their future today
